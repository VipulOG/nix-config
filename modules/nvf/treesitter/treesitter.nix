{
  config.vim.treesitter = {
    enable = true;
    autotagHtml = true;
    context = {
      enable = false;
      setupOpts = {
        line_numbers = false;
        max_lines = 4;
      };
    };
  };
}
